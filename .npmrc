# 工作区本地 pnpm store 意图文档（标准 pnpm 环境会读取此设置）。
# 注意：本沙箱环境的 pnpm 11.7 不读取 .npmrc 的 store-dir（pnpm config get store-dir 返回
# undefined），实际安装一律走 scripts/install.sh（显式 --store-dir，路径见该脚本）。
store-dir=.pnpm-store
